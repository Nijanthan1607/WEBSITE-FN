import React from 'react'

export default function FacultyDashboard() {
    return (
        <div className="card max-w-2xl mx-auto">
            <h2 className="text-xl font-semibold">FACULTY DASHBOARD</h2>
            <p className="mt-2">Attendance, request delay, announcements UI will be similar to student pages. Use Firestore collections to record requests and attendance.</p>
        </div>
    )
}
